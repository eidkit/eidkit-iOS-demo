import SwiftUI

// MARK: - Brand colours (mirrors Android theme)

extension Color {
    /// #2563EB — primary electric blue
    static let electricBlue      = Color(red: 0.145, green: 0.388, blue: 0.922)
    /// #60A5FA — lighter blue for icons / accents
    static let electricBlueLight = Color(red: 0.376, green: 0.647, blue: 0.980)
    /// #0F172A — dark background
    static let surfaceDark       = Color(red: 0.059, green: 0.090, blue: 0.165)
    /// #1E293B — card surface
    static let surfaceCard       = Color(red: 0.118, green: 0.161, blue: 0.231)
    /// #334155 — card border
    static let surfaceBorder     = Color(red: 0.200, green: 0.255, blue: 0.333)
    /// #22C55E — success green
    static let successGreen      = Color(red: 0.133, green: 0.773, blue: 0.369)
    /// #EF4444 — error red
    static let errorRed          = Color(red: 0.937, green: 0.267, blue: 0.267)
}
